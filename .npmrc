# Raise Node's heap ceiling for npm lifecycle scripts (install, postinstall, etc.).
# The default ~1.5 GB is not enough for the HCC frontend-components-config tree
# and causes SIGKILL (OOM) in Konflux CI pods.
node-options=--max-old-space-size=8192
